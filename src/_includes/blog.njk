---
layout: "base.njk"
---
<div class="mx-auto mt-10 px-4 pb-28 sm:mt-16 sm:px-6 md:px-8 xl:px-12 xl:max-w-6xl">
    <article class="relative pt-10 max-w-3xl mx-auto xl:max-w-none xl:grid xl:grid-cols-[1fr_50rem] xl:gap-x-8">
        <header class="col-span-full mb-10">
            <div class="col-span-full inset-x-0 text-slate-700 dark:text-slate-400 sm:text-center mb-2 text-sm">{{ date | postDateLong }}</div>

            <h1 class="col-span-full text-3xl sm:mb-6 sm:text-4xl sm:text-center xl:mb-16 font-extrabold tracking-tight text-slate-900 text-slate-200">
                {{ title }}
            </h1>

            <figure class="col-span-full">
                <img class="h-96 w-full rounded-lg object-cover" src="{{ cover | image(600) }}" alt="">
            </figure>
        </header>

        <div class="text-sm leading-6 mb-16 xl:mb-0">
            <div class="hidden mb-5 pb-5 border-b border-slate-200 xl:block dark:border-slate-200/5">
                <a class="group flex font-semibold text-slate-700 hover:text-slate-900 dark:text-slate-200 dark:hover:text-white" href="/{{ layouttype }}">
                    <svg viewBox="0 -9 3 24" class="overflow-visible mr-3 text-slate-400 w-auto h-6 group-hover:text-slate-600 dark:group-hover:text-slate-300">
                        <path d="M3 0L0 3L3 6" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"></path>
                    </svg>Go back to {{ layouttype }}</a>
            </div>

            <div class="sm:flex sm:flex-wrap sm:justify-center xl:block">
                <dt class="sr-only">Author</dt>
                <dd class="flex items-center font-medium mt-6 sm:mx-3 xl:mx-0"><img src="{{ avatar }}" alt="" class="mr-3 w-10 h-10 rounded-full bg-slate-50 dark:bg-slate-800">
                    <div>
                        <div class="text-slate-900 dark:text-slate-200">{{ author }}</div>
                        <a href="https://twitter.com/{{ twitter }}" class="text-sky-500 hover:text-sky-600 dark:text-sky-400">@{{ twitter }}</a>
                    </div>
                </dd>
            </div>
        </div>

        <div class="mx-auto prose prose-slate dark:prose-dark dark:prose-invert">
            {{ content | safe }}
        </div>

    </article>
</div>

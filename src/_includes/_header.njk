{% set navPages = collections.all | eleventyNavigation %}
<!-- This example requires Tailwind CSS v2.0+ -->
<header class="sticky top-0 z-50 bg-indigo-800">
  <nav class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8" aria-label="Top">
    <div class="w-full py-6 flex items-center justify-between border-b border-indigo-500 lg:border-none">
      <div class="flex items-center">
        <a href="/">
          <span class="sr-only">Castmill</span>
          <img class="h-10 w-auto" src="/images/logo.png" alt="">
        </a>
        <div class="hidden ml-10 space-x-8 lg:block">
          {%- for entry in navPages %}
            <a href="{{ entry.url | url }}" class="text-base font-medium text-white hover:text-indigo-50 {% if entry.url == page.url %}text-indigo-300{% endif %}">
              {{ entry.title }}</a>
          {%- endfor %}
        </div>
      </div>
      <div class="ml-10 space-x-4">
        <a href="#" class="inline-block bg-indigo-500 py-2 px-4 border border-transparent rounded-md text-base font-medium text-white hover:bg-opacity-75">Sign in</a>
        <a href="#" class="inline-block bg-white py-2 px-4 border border-transparent rounded-md text-base font-medium text-indigo-600 hover:bg-indigo-50">Sign up</a>
      </div>
    </div>
    <div class="py-4 flex flex-wrap justify-center space-x-6 lg:hidden">
      {%- for entry in navPages %}
        <a href="{{ entry.url | url }}" class="text-base font-medium text-white hover:text-indigo-50 {% if entry.url == page.url %}text-indigo-300{% endif %}">
          {{ entry.title }}</a>
      {%- endfor %}
    </div>
  </nav>
</header>